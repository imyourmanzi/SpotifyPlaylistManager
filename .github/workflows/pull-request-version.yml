name: 'Check Version'

on:
  pull_request:
    branches:
      - main
    paths:
      - 'packages/**'
      - 'package*.json'
      - 'tsconfig.base.json'
      - 'babel.config.json'
      - 'nx.json'
      - 'workspace.json'

jobs:
  check:
    name: Check
    uses: imyourmanzi/GitHubWorkflows/.github/workflows/check-version-npm.yml@main
